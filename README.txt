Olin Feist

C++ script to read CSV file and find weighted means


To Run script simple:
1. Open terminal and go to directory, then got to src
2 run: "g++ SumProduct.cpp -o SumProduct"
3. run: "./SumProduct"
4. Open weighted means text file
